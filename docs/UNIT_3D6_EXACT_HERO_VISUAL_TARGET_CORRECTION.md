# Unit 3D6 — Exact Hero Visual Target Correction

## 1. Status

Unit 3D6 corrects the dedicated V3 hero SVG to follow the Project Owner's actual visual target. This is a bounded SVG asset, small HTML/CSS integration, and documentation unit. JavaScript, page copy, dependencies, deployment state, and production infrastructure remain unchanged.

## 2. Purpose

The purpose of this unit is to replace the restrained dashboard-grid interpretation with a high-impact cyber-commerce composition centered on a glowing DaDaStore cart mark, concentric neon rings, a holographic platform, and surrounding system cards.

Core law:

- Exact visual target is not fake-claim permission.
- Hero visual correction is not full-site redesign.
- SVG asset is not an external dependency.
- Visual reference is not production cutover.
- No fake metrics.

## 3. Owner correction

The Project Owner clarified that the prior instructions over-constrained the hero by avoiding the centered logo/orbit structure. The intended target specifically uses:

- a central glowing cart mark
- concentric neon rings
- a holographic glowing base
- side-mounted dashboard and product-style cards
- intense cyan, pink, violet, and blue lighting
- a polished, high-impact dashboard scene

This clarification supersedes Unit 3D5's avoidance of a centered mark and orbital composition.

## 4. Authority reviewed

- `GENERIC_CODING_PROJECT_WORKFLOW_RULES.md`
- `docs/UNIT_3D5_DEDICATED_HERO_VISUAL_ASSET.md`
- `_local-reference/v3-hero-orbit-cart-target.png`
- `_local-reference/v3-template-reference-02.png`
- `assets/img/hero-commerce-intelligence.svg`
- `index.html`
- `assets/css/style.css`
- `assets/js/main.js`
- current Git repository state

The workflow authority and Unit 3D5 were read completely. The existing SVG was read completely. The primary hero target and secondary page reference were inspected at original resolution. Current integration and JavaScript hooks were inspected and preserved.

## 5. Branch

- Implementation branch: `design/commerce-intelligence-v3`
- No commit, push, merge, preview deployment, production deployment, or domain action is included.

## 6. Previous miss

Unit 3D5 correctly created a local, scalable, claim-safe SVG, but its composition remained a restrained grid of similarly weighted modules. It did not reproduce the target's central visual hierarchy, dimensional energy, or concentrated neon lighting.

The previous result treated the orbit/cart structure as something to avoid. The Project Owner's correction establishes that this structure is the defining feature of the desired hero.

## 7. Exact hero target

The corrected direction is a finished premium hero scene with:

- one dominant commerce-engine focal point
- a vector DaDaStore cart-inspired mark at the center
- several differently sized neon rings with segmented orbital arcs and energy nodes
- a rising holographic beam and abstract illuminated skyline
- stacked elliptical platform rings and a perspective floor
- three floating cards on each side
- dense stars, network lines, glows, and depth
- the established cyan, pink, violet, and electric-blue palette

The primary reference controls the hero composition. The wider template remains secondary guidance for page balance and integration.

## 8. SVG changes

`assets/img/hero-commerce-intelligence.svg` was rebuilt as a `960 × 620` vector scene.

The SVG now includes:

- a central vector cart assembled from SVG paths, strokes, basket divisions, and wheels
- cyan, pink, and violet glow around the cart
- three concentric rings plus segmented outer arcs and illuminated nodes
- a vertical holographic beam
- a neon bar skyline behind the platform
- four stacked platform ellipses, lower support arcs, and a perspective grid floor
- left cards for Acquisition, Creative, and Meta/Google/TikTok channels
- right cards for Shopify / CRO, Tracking / Analytics, and connected Signals
- abstract line paths, bars, storefront geometry, nodes, and offer cards
- a dark clipped scene with layered atmospheric glows and signal-network details

The cart is entirely vector-based. No raster logo, product image, client image, screenshot, or external asset is embedded.

## 9. HTML/CSS changes

`index.html` continues to reference:

```text
assets/img/hero-commerce-intelligence.svg
```

Its alt text now reads:

> Glowing DaDaStore commerce intelligence platform with neon rings, dashboard cards, and holographic growth system visual

No hero headline, CTA, proof value, section ID, mailto target, or page copy was rewritten.

The existing fluid `.hero-visual` wrapper remains. Its background and shadow were strengthened with a centered violet glow, blue glow, pink ambient glow, and layered shadow so the new SVG remains visually dominant without cropping or fixed-height clipping.

## 10. Proof/claim guardrails

The SVG uses neutral system labels only:

- Acquisition
- Creative
- Shopify / CRO
- Tracking
- Signals
- Offers
- Meta
- Google
- TikTok
- Analytics

It contains no financial figure, ratio, order count, sales claim, revenue claim, ad-spend claim, testimonial, client, client logo, rate, location, or unapproved contact address from the visual references.

Approved Upwork proof values remain in the surrounding HTML and were not added to the SVG.

## 11. JS compatibility preserved

`assets/js/main.js` is unchanged. The correction preserves:

- `[data-header]`
- `[data-nav]`
- `[data-nav-toggle]`
- `.reveal`
- `[data-back-to-top]`
- `[data-year]`
- Services, Process, Proof, Work, and Contact navigation targets
- mobile navigation
- smooth internal scrolling
- active navigation state
- reveal behavior
- back-to-top behavior

No runtime request, event behavior, analytics call, dependency, or contact backend was introduced.

## 12. Responsive considerations

Static layout checks cover:

- `375px` and `390px`: the hero remains single-column, the complete SVG scales below the text, the central mark remains centered, CTA buttons remain visible, and no fixed SVG width is introduced.
- `768px`: the existing fluid split-grid layout places the visual in a `minmax()` track while preserving the proof bar below both columns.
- `1024px`: the visual occupies the larger hero track and retains its full uncropped aspect ratio.
- `1440px`: the capped page container keeps the denser visual balanced beside the hero copy.

The asset uses a responsive `viewBox`; the image uses `width: 100%`, `height: auto`, and `object-fit: contain`. The wrapper retains `min-width: 0` and contained overflow. Side cards are inside the SVG viewBox and cannot create page-level horizontal scrolling.

## 13. Files changed

Changed:

- `assets/img/hero-commerce-intelligence.svg`
- `index.html`
- `assets/css/style.css`

Added:

- `docs/UNIT_3D6_EXACT_HERO_VISUAL_TARGET_CORRECTION.md`

Not changed:

- `assets/js/main.js`
- raster images
- existing logo and favicon assets
- dependency or build configuration
- analytics or contact behavior
- private reference images

## 14. Verification record

- Current branch is `design/commerce-intelligence-v3`.
- The SVG exists and parses as valid XML.
- The SVG contains every required neutral system-label category.
- The SVG contains none of Unit 3D6's forbidden claims.
- No tracked PNG, JPG, JPEG, WebP, or GIF file has a diff.
- `index.html` references the SVG and uses the corrected alt text.
- Required section IDs remain present and unique.
- All mailto links continue to target `mailto:dadastore.co@gmail.com`.
- `assets/js/main.js` has no Git diff.
- No external font, URL dependency, framework, library, analytics integration, or contact backend was added.
- `git diff --check` is used for final whitespace validation.

## 15. Known limitations

- Static source verification is not browser, physical-device, or Project Owner visual approval.
- The central mark approximates the DaDaStore cart style in original SVG geometry; it is not an embedded copy of the raster logo.
- The surrounding cards are abstract system visuals, not live dashboards or software.
- Smaller secondary card labels naturally reduce in size when the complete composition is scaled to a narrow mobile viewport; the central mark and overall scene remain the visual focus.
- SVG filters can render with small differences between browser engines and graphics environments.
- No formal browser, screen-reader, Lighthouse, console, network, social-preview, or performance result is claimed.
- The branch is not merged, deployed, or approved for production.

## 16. Recommended next unit

**Unit 3E2 — Post-Hero Visual Browser QA and Visual Review**

Goal: verify the corrected hero scene and current page across required browsers and viewport widths, including visual similarity, central-mark prominence, ring and glow rendering, side-card clipping, image loading, overflow, CTA visibility, proof-bar placement, navigation behavior, console/network state, reduced motion, and accessibility foundations before owner approval.

## 17. Explicitly not next

- another broad hero or page redesign before browser and owner review
- fake metrics, live dashboard behavior, client claims, testimonials, or product images
- JavaScript feature expansion
- raster image generation
- external fonts, libraries, frameworks, or dependencies
- analytics or contact backend
- case-study publication
- merge to `main`
- preview or production deployment
- DNS, CNAME, or custom-domain changes
- live DaDaStore repository or site modification

## 18. Test record

No automated application test suite is required for this bounded SVG/HTML/CSS/docs correction. Static SVG validity, forbidden-content, asset-integrity, HTML reference, section-ID, mailto, JavaScript-integrity, responsive-rule, dependency, and Git checks are used. Browser visual QA remains deferred to Unit 3E2.
